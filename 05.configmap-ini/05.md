Create a ConfigMap where each key is the name of a file, and the associated 
value is the entire contet of the file.
The files are located in a directory called ini-files.

ConfigMap name: ini-config
